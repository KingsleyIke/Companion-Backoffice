import 'package:flutter/material.dart';

class AddReadingsPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
          "Add Readings"
      ),
    );
  }

}